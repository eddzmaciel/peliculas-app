import { PeliculaImagePipe } from './pelicula-image.pipe';

describe('PeliculaImagePipe', () => {
  it('create an instance', () => {
    const pipe = new PeliculaImagePipe();
    expect(pipe).toBeTruthy();
  });
});
